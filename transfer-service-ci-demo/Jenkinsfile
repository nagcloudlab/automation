pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
              sh 'cd transfer-service-ci-demo/api-tests'
              sh 'mvn clean test-compile'
            }
        }
        stage('Test') {
            steps {
              echo 'Testing...'
            }
        }
    }
}